<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <!-- VPN -->
    <uses-permission android:name="android.permission.BIND_VPN_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Wake lock to keep tunnel alive -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Biometric app-lock (BiometricPrompt). Also merged from androidx.biometric,
         declared explicitly so the fingerprint/face prompt can never be missing. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <!-- In-app APK self-update (DownloadManager + package installer prompt) -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Audio calling (react-native-webrtc): mic access, audio routing
         (speaker/earpiece/bluetooth), and a microphone-type foreground
         service so an active call survives the app being backgrounded,
         same pattern as the VPN tunnel's own foreground service above. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- Video calling (phase 2, not enabled yet — Khabat, 2026-07-28:
         build it now so it's ready once audio is live and there's real
         relay-rate data to decide on). Camera + a camera-type foreground
         service, same posture as the microphone one above. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.front" android:required="false" />

    <!-- Screen sharing in calls (Khabat, 2026-08-01 spec, behind
         realtime_screen_sharing_enabled). react-native-webrtc's own
         AndroidManifest.xml (node_modules/react-native-webrtc/android/
         src/main/AndroidManifest.xml) already declares the
         MediaProjectionService component with
         foregroundServiceType="mediaProjection" -- that merges in
         automatically via Gradle. This permission is the one piece the
         *app's* manifest still has to declare itself: Android 14/API 34
         requires FOREGROUND_SERVICE_MEDIA_PROJECTION explicitly, the
         service's own foregroundServiceType isn't sufficient alone. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <!-- Package visibility (Android 11+): needed so the Smart Mode
         "Bypass selected apps" screen can list launchable apps. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/full_backup_content"
        android:extractNativeLibs="true"
        android:theme="@style/AppTheme"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- AdMob APPLICATION_ID is injected by react-native-google-mobile-ads from
             app.json (react-native-google-mobile-ads.android_app_id) — do NOT declare
             it here too, or the manifest merger fails with a duplicate. -->

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep links: setalink://... -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="setalink" />
            </intent-filter>

            <!-- App Links: https://setalink.no/?ref=CODE and /invite/CODE open the
                 app with the referral attached when installed; the website
                 handles them when not. Verification needs
                 /.well-known/assetlinks.json with the release-cert SHA-256. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="setalink.no" />
            </intent-filter>
        </activity>

        <!-- Exposes downloaded APK updates to the system package installer -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.setalink.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Xray VPN service -->
        <service
            android:name=".vpn.XrayVpnService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

    </application>
</manifest>
