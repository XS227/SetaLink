org.gradle.jvmargs=-Xmx512m -XX:MaxMetaspaceSize=192m -XX:+HeapDumpOnOutOfMemoryError
kotlin.daemon.jvm.options=-Xmx256m -XX:MaxMetaspaceSize=96m
org.gradle.parallel=false
org.gradle.caching=false
org.gradle.daemon=false

android.useAndroidX=true
android.enableJetifier=false

# New Architecture — disabled for alpha; re-enable after Xray native module matures
newArchEnabled=false
hermesEnabled=true
