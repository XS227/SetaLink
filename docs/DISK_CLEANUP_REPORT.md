# SetaLink Disk Cleanup Report — 2026-06-14 15:30 UTC

Performed on `/var/www/setalink` (host `/dev/vda1`, 24G). **No git history rewritten, no
services restarted, no production release deleted.**

## Result

| | Before | After |
|---|---|---|
| Disk `/` used | 74% (17G, 6.2G free) | **66% (8.1G free)** |
| Repo `/var/www/setalink` | 5.1G | **3.1G** |

> Quarantine deleted 2026-06-14 15:36 UTC after live download/OTA targets re-verified — reclaimed the ~1.17G.

> ⚠️ `/home` and `/var/www` are the **same partition** (`/dev/vda1`). Files moved to the
> quarantine therefore still occupy disk — the remaining ~1.17G is only reclaimed once the
> quarantine dir is deleted (see "Pending" below).

## A — Deleted (gitignored, regenerable) — frees disk now, ~0.8G

| Path | Size | Why safe |
|---|---|---|
| `mobile-app/android/app/build/` | 789M | Gradle build output, `git check-ignore` = ignored, regenerated by next build |
| `mobile-app/android/.gradle/` | 14M | Gradle cache, regenerable |
| `mobile-app/android/app/.cxx/` | ~1M | Native build cache, regenerable |

## B — Quarantined (obsolete, gitignored, zero references) — ~1.17G

Moved to `/home/ubuntu/setalink-cleanup-quarantine/20260614-1530/` (paths mirrored for restore).

- **B1 — `public/assets/*.apk` (11 files, 562M):** legacy staging dir. `grep` across
  HTML/PHP/JS/JSON and `/etc/nginx` found **no references**. Held ancient v0.9.10–v0.9.23
  plus duplicates of v0.9.28–v0.9.32 already present in `releases/stable/`.
- **B2 — old `public/releases/stable/*.apk` (11 files, 605M):** versions **not** referenced
  by `version.json` — v0.9.26, v0.9.27, v0.9.29, v0.9.30, v0.9.31 (all variants).

## Preserved (untouched)

- `releases/stable/`: **v0.9.32** (apk/arm32/universal) — latest, symlink + version.json target.
- `releases/stable/`: **v0.9.28** (apk/arm32/universal) — `experimental` channel in version.json.
- `releases/stable/` + `download/`: all `setalink-latest*` symlinks.
- `releases/hotfix/setalink-v0.9.9-hotfix.apk` + its symlink.
- `public/download/version.json`, `public/assets/logo/`, all source, `.git/objects`.

## NOT done — and why

- **`git lfs prune` (would free ~982M from `.git/lfs`):** **aborted.** Run with
  `--verify-remote`; GitHub auth is unavailable in this environment and all 18 objects were
  reported *"missing on remote."* Since origin cannot be confirmed to hold a copy, pruning
  would risk **permanent loss** of historical APK blobs. Left intact.

## Verification (all passed)

- All `download/` + `releases/stable/` + `hotfix/` symlinks resolve to existing files.
- All 7 `version.json` APK URLs (stable v0.9.32 + experimental v0.9.28) resolve to existing files.
- `setalink-v0.9.32.apk` sha256 = `c06d3f3b07371855376554f17c86290d25852b63bc3b75ec9dcd98f985ca7312` (matches version.json).
- `git status` unchanged from session start (all moved/deleted files were gitignored).

## Pending (needs operator decision)

1. ~~Reclaim ~1.17G from quarantine~~ — **done** 2026-06-14 15:36 UTC.
2. **Reclaim ~982M (`.git/lfs`):** still pending. Only after confirming those 18 LFS objects are
   pushed to `origin` (run `git lfs push --all origin`, then `git lfs prune --verify-remote`).
